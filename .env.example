# ===========================================
# Copy this file to .env and .env.local, then fill in real values.
# ===========================================

# --- Local development with SQLite (no Neon required) ---
# Use this if prisma/schema.prisma has provider = "sqlite".
# The URL MUST start with "file:" for SQLite.
DATABASE_URL="file:./prisma/dev.db"

# --- Production: Neon PostgreSQL ---
# Get both URLs from https://console.neon.tech → your project → Connection details
# Use "Pooled connection" for DATABASE_URL (host contains -pooler).
# Use "Connection string" (direct, no -pooler) for DIRECT_URL (Prisma migrations).
# DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Auth (use long random string in production: openssl rand -base64 32)
NEXTAUTH_SECRET="change-in-production"
AUTH_SECRET="change-in-production"
NEXTAUTH_URL="http://localhost:3000"
AUTH_URL="http://localhost:3000"

# Optional: APIs
GEMINI_API_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Feature flags
NEXT_PUBLIC_ENABLE_AUTH="true"
NEXT_PUBLIC_ENABLE_CREDITS="true"
